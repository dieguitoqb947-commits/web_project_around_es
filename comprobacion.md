TypeScript + HTML/CSS Interacción con la API Se creó una instancia de la clase descripción del proyecto. Se creó la clase Api una sola vez. La clase coincide con la Api , en la cual deben describirse las solicitudes al servidor. Las solicitudes al servidor no deben describirse dentro de otras clases ni de index.ts . Cada método que realiza solicitudes al servidor está marcado como y utiliza await para gestionar la respuesta. async Las operaciones sobre el DOM y la lógica de la aplicación se ejecutan de forma secuencial tras esperar la respuesta de la API. Siempre se debe comprobar que la respuesta del servidor sea correcta: if (res.ok) { return await res.json(); 3 Lista de comprobación. Proyecto del sprint 9: Alrededor de los EE.UU., parte 4 — Integración de la API} throw new Error(`Error: ${res.status}`); Se utiliza el bloque try...catch para procesar los errores de todas las solicitudes al servidor. No hay instancias de otras clases dentro de la clase Api . Dentro de Api no se llama a métodos de otras clases. Se usa el acoplamiento bajo o débil entre clases. Ventanas modales: Las ventanas emergentes se crearon con HTML y CSS. No deberían crearse de forma dinámica mediante TS/JS. Los cuadros emergentes pueden cerrarse en cualquier resolución de pantalla. El cuadro emergente con la imagen se abre correctamente, y las imágenes se muestran respetando las proporciones de aspecto. La ventana emergente se puede cerrar al hacer clic en cualquier lugar fuera de sus bordes, o al pulsar la tecla Esc. La ventana emergente no se cierra al hacer clic dentro de sus bordes (en el propio formulario, no en la ventana). Validación: Todos los campos de entrada son validados por una función universal. Una función por separado controla el estado del botón La función Submit . enableValidation() es la que activa la validación. El botón Submit está inactivo si al menos uno de los campos no pasa la validación. POO Se utilizan clases de ES6. Cada clase se describe en un archivo TS por separado y se importa a index.ts . 4 Lista de comprobación. Proyecto del sprint 9: Alrededor de los EE.UU., parte 4 — Integración de la APICada clase debe realizar solo una tarea específica. - Toda lógica relacionada a una tarea específica debe estar encapsulada en su respectiva clase. No se necesita ninguna otra clase específica para realizar esta tarea. Se utiliza el acoplamiento flexible o débil. No se crean instancias de otras clases dentro de una clase. Se crea una instancia de la clase Section para cada contenedor, donde se renderizan los elementos. La clase coincide con la descripción del proyecto. Se crea una instancia de la clase Card para cada tarjeta. La clase coincide con la descripción del proyecto. Se crea una instancia de la clase FormValidator para cada formulario que deba ser validado. La clase coincide con la descripción del proyecto. Se crea una instancia de UserInfo una sola vez. La clase coincide con la descripción del proyecto. La clase Popup es una clase padre, la cual tiene tres subclases que fueron creadas para cada ventana emergente (popup). La clase y sus subclases coinciden con la descripción del proyecto. Se agregó otro tipo de ventana emergente para confirmar que la tarjeta se eliminó. Se le puede asignar un submission handler (controlador de envío) para establecer el El código está optimizado: id de la tarjeta. No debe asignarse ningún dato que introduzca el usuario a la propiedad innerHTML . No hay código duplicado. Si una línea de código tiene que repetirse, se debe escribir como una función separada. Si se declara una variable utilizando lugar. let , su valor debe cambiar en algún Si el valor de una variable no cambia, debe declararse con const . Todos los valores numéricos se asignan a variables. Los valores únicos que no tienen su propia variable se llaman "números mágicos", y se consideran una mala práctica en programación. 5 Lista de comprobación. Proyecto del sprint 9: Alrededor de los EE.UU., parte 4 — Integración de la APILas operaciones en elementos del DOM se ejecutan antes de que se inserten en el diseño. Una función realiza una sola tarea, por ejemplo, devuelve la tarjeta del código HTML. Cuando se abre una ventana emergente (popup), se añade un detector de eventos que permite cerrarla pulsando Esc ; y cuando la ventana se cierra, este detector se elimina.