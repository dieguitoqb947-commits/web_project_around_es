Lista de comprobación: Proyecto 8 Requisitos del proyecto General Tu proyecto contiene: Un archivo index.html . Un archivo index.ts . Los siguientes scripts: FormValidator.ts , PopupWithForm.ts , Card.ts , Section.ts , PopupWithImage.ts y UserInfo.ts . Un archivo README.md . Los archivos TS están almacenados en la carpeta src . Popup.ts , El formateo del código es consistente y está indentado de acuerdo con las mejores prácticas de anidado. El archivo README.md contiene: El nombre del proyecto. Una descripción del proyecto y su funcionalidad. Una descripción de las tecnologías y técnicas utilizadas. El proyecto cumple con los siguientes requisitos de estilo del código Se utiliza camelCase para los nombres de funciones y variables. Solo se utilizan sustantivos para los nombres de variables. Los nombres de variables describen con claridad lo que almacenan. Si el proyecto tiene varias variables con datos similares, esas variables tienen nombres únicos, pero descriptivos. 1 Lista de comprobación: Proyecto 8Se utilizan sustantivos plurales para las colecciones de NodeList. Se utilizan nombres descriptivos para las funciones, que reflejan claramente lo que hacen. Los nombres de funciones comienzan con un verbo. Los nombres no deben incluir abreviaturas inapropiadas o poco claras. La funcionalidad ha sido implementada según el brief Se han creado 6 tarjetas a través de TS/JS. El formulario para agregar tarjetas ha sido programado correctamente en HTML y CSS; el formulario puede abrirse y, al enviarlo, se agrega una tarjeta. El botón "Me gusta" es funcional. Se ha implementado correctamente la función para eliminar tarjetas. Mientras un campo de texto se encuentre activo, se puede agregar una tarjeta presionando Enter. TypeScript + HTML/CSS Ventanas modales: Las ventanas emergentes se crearon con HTML y CSS. No deberían crearse de forma dinámica mediante TS/JS. Los cuadros emergentes pueden cerrarse en cualquier resolución de pantalla. El cuadro emergente con la imagen se abre correctamente, y las imágenes se muestran respetando las proporciones de aspecto. La ventana emergente se puede cerrar al hacer clic en cualquier lugar fuera de sus bordes, o al pulsar la tecla Esc. La ventana emergente no se cierra al hacer clic dentro de sus bordes (en el propio formulario, no en la ventana). Validación: Todos los campos de entrada son validados por una función universal. Una función por separado controla el estado del botón Submit . 2 Lista de comprobación: Proyecto 8La función enableValidation() es la que activa la validación. El botón Submit está inactivo si al menos uno de los campos no pasa la validación. POO Se utilizan clases de ES6. Cada clase se describe en un archivo TS por separado y se importa a index.ts . Cada clase debe realizar solo una tarea específica. Toda lógica relacionada con una tarea específica debe estar encapsulada en su respectiva clase. No se necesita ninguna otra clase específica para realizar esta tarea. Se utiliza el acoplamiento flexible o débil. No se crean instancias de otras clases dentro de una clase. Se crea una instancia de la clase Section para cada contenedor, donde se renderizan los elementos. La clase coincide con la descripción del proyecto. Se crea una instancia de la clase la descripción del proyecto. Card para cada tarjeta. La clase coincide con Se crea una instancia de la clase FormValidator para cada formulario que deba ser validado. La clase coincide con la descripción del proyecto. Se crea una instancia de UserInfo una sola vez. La clase coincide con la descripción del proyecto. La clase Popup es una clase padre, la cual tiene dos subclases que han sido creadas para cada cuadro emergente. La clase y sus subclases coinciden con la descripción del proyecto. El código está optimizado: No debe asignarse ningún dato que introduzca el usuario a la propiedad innerHTML . No hay código duplicado. Si una línea de código tiene que repetirse, se debe escribir como una función separada. Si se declara una variable utilizando lugar. let , su valor debe cambiar en algún 3 Lista de comprobación: Proyecto 8Si el valor de una variable no cambia, debe declararse con const . Todos los valores numéricos se asignan a variables. Los valores únicos que no tienen su propia variable se llaman "números mágicos", y se consideran una mala práctica en programación. Las operaciones en elementos del DOM se ejecutan antes de que se inserten en el diseño. Una función realiza una sola tarea, por ejemplo, devuelve la tarjeta del código HTML. Cuando se abre una ventana emergente (popup), se añade un detector de eventos que permite cerrarla pulsando Esc ; y cuando la ventana se cierra, este detector se elimina.